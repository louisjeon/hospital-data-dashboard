import { Stack } from "@chakra-ui/react";

export const Main = (props) => (
  <Stack
    spacing="1.5rem"
    width="100%"
    maxWidth="48rem"
    pt="1rem"
    pb="3rem"
    px="1rem"
    {...props}
  />
);
